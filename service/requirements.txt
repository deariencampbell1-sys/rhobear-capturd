# Captur'd hosted service — API layer.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27

# The render worker shells the Captur'd engine (film.py). The host/container
# also needs the engine + its runtime:
#   pip install "capturd[voice]"   (from the sunsponge-capture repo/wheel)
#   playwright install chromium
#   ffmpeg on PATH (or bundled at packaging/ffmpeg/)
# See DEPLOY.md.
#
# psycopg is NOT in this base file on purpose: it is the Postgres *target* of
# the sqlite->Postgres migrator (ADR-0001 Stage 5) and is optional at runtime —
# the service keeps using sqlite until CAPTURD_DATABASE_URL is set. Only the
# box that actually runs `migrate_sqlite_to_postgres.py` needs it; install via
#   pip install -r requirements-postgres.txt
